# MCP server
mcp

# Windows system/process/disk/network inspection
psutil

# Windows Event Log access (services.py uses psutil's built-in win_service_* API instead)
pywin32; sys_platform == "win32"

# Lightweight LangChain test client (test_client.py)
langchain
langchain-ollama
langchain-mcp-adapters
